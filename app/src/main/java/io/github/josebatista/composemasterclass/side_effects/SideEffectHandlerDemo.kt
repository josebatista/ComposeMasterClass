package io.github.josebatista.composemasterclass.side_effects

import androidx.compose.runtime.Composable
import androidx.compose.runtime.SideEffect
import androidx.compose.ui.Modifier

@Composable
fun SideEffectHandlerDemo(modifier: Modifier = Modifier) {
    /*
    SideEffect is a useful tool for integrating Compose with code that is not Compose,
    allowing you to react to changes in the Compose state and execute actions outside of
    the Compose scope.
     */
    SideEffect {

    }
}
